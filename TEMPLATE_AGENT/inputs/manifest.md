# Inputs manifest — <AGENT_ID> (auto-synced by sync.sh)

<!-- Tiny version-pin roll-up. `sync.sh <AGENT_ID>` copies each producer's
     outputs/manifest.md into ./inputs/<PRODUCER>.md AND rewrites this file's pin
     table. Read THIS at boot for drift-check; open the full ./inputs/<PRODUCER>.md
     blob only on-demand when consuming an artifact. Entry-point agent (no
     producer): this table is empty. -->

## Pinned producer versions
- <PRODUCER>: 0.0.0   (copied from <PRODUCER>/outputs/manifest.md)
